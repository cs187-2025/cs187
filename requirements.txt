# ==============================================================================
# CS187 Materials - Python Package Requirements
# ==============================================================================
# Merged requirements from autograder/requirements.txt and Makefile env target
# Pinned versions are used where specified in either source

# Core Python packaging tools
# pip version pinned to avoid deprecation warnings from legacy packages
# Several dependencies (nltk, mysql-connector, wget, db-sqlite3, func_timeout,
# antiorm, db) use legacy setup.py build systems that will break in pip 25.3+
# See: https://github.com/pypa/pip/issues/6334
# TODO: Remove this pin when packages migrate to PEP 517 or find alternatives
pip>=23.0.1,<25.3        # Package installer (pinned to avoid PEP 517 breakage)
wheel                    # Built package format
setuptools==69.5.1       # Package build system

# Autograding and notebook infrastructure
otter-grader==1.0.0      # Automated grading system for Jupyter notebooks
jupyterlab==4.2.5        # Modern Jupyter development environment
jupyter-client==8.6.2    # Jupyter protocol client
jupyter-server==2.14.2   # Jupyter server backend
ipywidgets==8.1.5        # Interactive widgets for notebooks
nbconvert==7.16.4        # Convert notebooks to various formats
nbconvert[webpdf]         # Includes WebPDF dependencies (modern PDF generation)
nbformat                 # Jupyter notebook format libraries
mistune<3.0,>=2.0       # Markdown parser (pinned for nbconvert compatibility)
jupytext                 # Convert between notebook and text formats
ipykernel                # Python kernel for Jupyter
ipython>=7.0.0,<8.0.0    # Interactive Python shell (pinned to 7.x for otter-grader 1.0.0 compatibility)

# Classic notebook interface (provides jupyter-notebook executable)
notebook                 # Provides jupyter-notebook executable
playwright               # Browser automation for WebPDF conversion (requires Chromium)

# Core data science stack
pandas                   # Data manipulation and analysis
numpy                    # Numerical computing foundation
scipy                    # Scientific computing algorithms
scikit-learn            # Machine learning library
matplotlib               # Plotting and visualization
seaborn                  # Statistical data visualization
tqdm>=4.29.1            # Progress bars (with notebook support)

# Machine learning and deep learning
torch>=2.5.0,<2.8.0     # PyTorch for deep learning (compatible with Python 3.10+)
torchvision>=0.20.0,<0.23.0  # PyTorch computer vision library
torchaudio>=2.5.0,<2.8.0     # PyTorch audio processing library  
transformers            # Hugging Face transformers (LLM modules)
tokenizers              # Fast tokenizers for transformers
datasets                # Hugging Face datasets library

# Natural language processing
nltk==3.5               # Natural language toolkit
dateparser              # Parse dates from natural language

# Template and web utilities
markupsafe==2.0.1       # Safe string handling for templates
jinja2==3.0.3           # Template engine
tornado                 # Web framework components
cryptography            # Cryptographic utilities

# Data storage and retrieval
mysql-connector         # MySQL database connector
dill                    # Extended pickling capabilities
datascience             # Berkeley data science utilities
wget                    # File downloading utility
graphviz                # Python interface to graphviz graph visualization software
db-sqlite3              # SQLite database interface
func_timeout            # Function timeout utilities 